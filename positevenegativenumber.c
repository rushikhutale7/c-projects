#include<stdio.h>
#include<conio.h>
void main()
{
    int number;
    printf("enter the value of number\n");
    scanf("%d",&number);
    if(number>0)
    {
        printf("the given number is positive number \n");

    }
    else
    {
        printf("the given number is negative one \n");
    }



}